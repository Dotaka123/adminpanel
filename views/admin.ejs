<!DOCTYPE html>
<html>
<head>
    <title>Admin - ProxyFlow</title>
    <style>
        body { font-family: 'Segoe UI', sans-serif; background: #121212; color: white; padding: 30px; }
        .card { background: #1e1e1e; padding: 20px; border-radius: 12px; border: 1px solid #333; margin-bottom: 20px; }
        table { width: 100%; border-collapse: collapse; margin-top: 15px; }
        th { background: #333; padding: 12px; text-align: left; }
        td { padding: 12px; border-bottom: 1px solid #333; vertical-align: middle; }
        .badge { padding: 5px 10px; border-radius: 4px; font-size: 12px; font-weight: bold; }
        .badge-binance { background: #f3ba2f; color: black; }
        .badge-ltc { background: #345d9d; color: white; }
        .input-proxy { background: #2b2b2b; border: 1px solid #444; color: white; padding: 8px; width: 200px; border-radius: 4px; }
        .btn-accept { background: #2ecc71; color: white; border: none; padding: 8px 15px; border-radius: 4px; cursor: pointer; }
        .btn-reject { background: #e74c3c; color: white; border: none; padding: 8px 15px; border-radius: 4px; cursor: pointer; margin-left: 5px; }
    </style>
</head>
<body>
    <h1>🛠 Gestion des Commandes Pending</h1>

    <div class="card">
        <table>
            <thead>
                <tr>
                    <th>Utilisateur (PSID)</th>
                    <th>Méthode</th>
                    <th>Détails Paiement (ID Binance/LTC)</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                <% pendingOrders.forEach(order => { %>
                    <tr>
                        <td><code><%= order.psid %></code></td>
                        <td>
                            <span class="badge <%= order.method === 'BINANCE' ? 'badge-binance' : 'badge-ltc' %>">
                                <%= order.method %>
                            </span>
                        </td>
                        <td><strong><%= order.paymentRef || "Non fourni" %></strong></td>
                        <td>
                            <div style="display: flex; align-items: center;">
                                <form action="/admin/deliver" method="POST" style="margin-right: 10px;">
                                    <input type="hidden" name="orderId" value="<%= order.orderId %>">
                                    <input type="text" name="proxyData" class="input-proxy" placeholder="IP:PORT:USER:PASS" required>
                                    <button type="submit" class="btn-accept">Accepter & Livrer</button>
                                </form>
                                <form action="/admin/reject" method="POST">
                                    <input type="hidden" name="orderId" value="<%= order.orderId %>">
                                    <button type="submit" class="btn-reject" onclick="return confirm('Refuser cette commande ?')">Refuser</button>
                                </form>
                            </div>
                        </td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
    </div>
</body>
</html>
